apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
# - configmap.yaml
# - cronjob.yaml

commonLabels:
  app: freshrss

images:
  - name: freshrss/freshrss
    newTag: 1.23.1
